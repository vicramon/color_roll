#landing_page
  %section#top
    %h1
      %span.color><
        Color
      %span.roll Roll

      Notation
    %h2 By Vic Ramon
  %section#song_select
    .title Select a Song
    = form_tag song_path, method: :get do
      = select_tag :file, options_for_select(@files)
      .submit
        = submit_tag "Generate ColorRoll"
